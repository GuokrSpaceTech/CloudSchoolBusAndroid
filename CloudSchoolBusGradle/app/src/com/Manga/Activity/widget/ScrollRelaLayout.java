package com.Manga.Activity.widget;import android.content.Context;import android.util.AttributeSet;import android.widget.RelativeLayout;import android.widget.Scroller;public class ScrollRelaLayout extends RelativeLayout {	Scroller mScroller = null;	private boolean isScroll = true;	//private ToolInterface toolInterface;	public ScrollRelaLayout(Context context) {		super(context);		// TODO Auto-generated constructor stub		mScroller = new Scroller(context);	}	public ScrollRelaLayout(Context context, AttributeSet attrs) {		super(context, attrs);		// TODO Auto-generated constructor stub		mScroller = new Scroller(context);	}	@Override	public void computeScroll() {		// TODO Auto-generated method stub		super.computeScroll();		if (mScroller.computeScrollOffset()) {			scrollTo(mScroller.getCurrX(), 0);			postInvalidate();		}	}	public void beginScroll(int tmp) {		if (isScroll) {			mScroller.startScroll(0, 0, 0-tmp, 0, 1000);			isScroll = false;			//toolInterface.flingLeft();		} else {			mScroller.startScroll(0-tmp, 0,tmp, 0, 1000);			isScroll = true;			//toolInterface.flingRight();		}		invalidate();	}}