package com.guokrspace.cloudschoolbus.teacher.database.daodb;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table UPLOADING_PHOTO_ENTITY.
*/
public class UploadingPhotoEntityDao extends AbstractDao<UploadingPhotoEntity, String> {

    public static final String TABLENAME = "UPLOADING_PHOTO_ENTITY";

    /**
     * Properties of entity UploadingPhotoEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Key = new Property(0, String.class, "key", true, "KEY");
        public final static Property PicPathString = new Property(1, String.class, "picPathString", false, "PIC_PATH_STRING");
        public final static Property PicFileString = new Property(2, String.class, "picFileString", false, "PIC_FILE_STRING");
        public final static Property PicSizeString = new Property(3, String.class, "picSizeString", false, "PIC_SIZE_STRING");
        public final static Property StudentId = new Property(4, String.class, "studentId", false, "STUDENT_ID");
        public final static Property Classuid = new Property(5, String.class, "classuid", false, "CLASSUID");
        public final static Property Intro = new Property(6, String.class, "intro", false, "INTRO");
        public final static Property PhotoTag = new Property(7, String.class, "photoTag", false, "PHOTO_TAG");
        public final static Property Teacherid = new Property(8, String.class, "teacherid", false, "TEACHERID");
    };


    public UploadingPhotoEntityDao(DaoConfig config) {
        super(config);
    }
    
    public UploadingPhotoEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'UPLOADING_PHOTO_ENTITY' (" + //
                "'KEY' TEXT PRIMARY KEY NOT NULL ," + // 0: key
                "'PIC_PATH_STRING' TEXT," + // 1: picPathString
                "'PIC_FILE_STRING' TEXT," + // 2: picFileString
                "'PIC_SIZE_STRING' TEXT," + // 3: picSizeString
                "'STUDENT_ID' TEXT," + // 4: studentId
                "'CLASSUID' TEXT," + // 5: classuid
                "'INTRO' TEXT," + // 6: intro
                "'PHOTO_TAG' TEXT," + // 7: photoTag
                "'TEACHERID' TEXT);"); // 8: teacherid
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'UPLOADING_PHOTO_ENTITY'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, UploadingPhotoEntity entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.getKey());
 
        String picPathString = entity.getPicPathString();
        if (picPathString != null) {
            stmt.bindString(2, picPathString);
        }
 
        String picFileString = entity.getPicFileString();
        if (picFileString != null) {
            stmt.bindString(3, picFileString);
        }
 
        String picSizeString = entity.getPicSizeString();
        if (picSizeString != null) {
            stmt.bindString(4, picSizeString);
        }
 
        String studentId = entity.getStudentId();
        if (studentId != null) {
            stmt.bindString(5, studentId);
        }
 
        String classuid = entity.getClassuid();
        if (classuid != null) {
            stmt.bindString(6, classuid);
        }
 
        String intro = entity.getIntro();
        if (intro != null) {
            stmt.bindString(7, intro);
        }
 
        String photoTag = entity.getPhotoTag();
        if (photoTag != null) {
            stmt.bindString(8, photoTag);
        }
 
        String teacherid = entity.getTeacherid();
        if (teacherid != null) {
            stmt.bindString(9, teacherid);
        }
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public UploadingPhotoEntity readEntity(Cursor cursor, int offset) {
        UploadingPhotoEntity entity = new UploadingPhotoEntity( //
            cursor.getString(offset + 0), // key
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // picPathString
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // picFileString
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // picSizeString
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // studentId
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // classuid
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // intro
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // photoTag
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8) // teacherid
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, UploadingPhotoEntity entity, int offset) {
        entity.setKey(cursor.getString(offset + 0));
        entity.setPicPathString(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPicFileString(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setPicSizeString(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setStudentId(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setClassuid(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setIntro(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setPhotoTag(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setTeacherid(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(UploadingPhotoEntity entity, long rowId) {
        return entity.getKey();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(UploadingPhotoEntity entity) {
        if(entity != null) {
            return entity.getKey();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
